{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm build --filter @brotherssportszone/admin",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
